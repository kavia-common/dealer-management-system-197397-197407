import React from "react";
import PagePlaceholder from "../components/PagePlaceholder";

// PUBLIC_INTERFACE
export default function PayrollCreditsPage() {
  return (
    <PagePlaceholder
      title="Payroll / Credits"
      description="Manage dealer payroll, credits, and outstanding amounts."
    />
  );
}
