import React from "react";
import PagePlaceholder from "../components/PagePlaceholder";

// PUBLIC_INTERFACE
export default function StockPage() {
  return (
    <PagePlaceholder
      title="Stock"
      description="Track stock received from dealers, quantities, and inventory movements."
    />
  );
}
